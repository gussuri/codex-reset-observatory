# Agent Guidance

## Execution

- 明確な実装依頼は、追加の確認や承認を基本的に求めず、調査・実装・検証まで自律的に進める。
- 依頼に commit・push が含まれる場合は、検証が成功したらそのまま実行する。
- 明確な実装依頼では、ユーザーがcommit・pushをしないよう指定していない限り、検証成功後にcommit・pushまで実行する。
- 途中経過は簡潔に共有するが、仕様が具体的な作業を確認待ちで止めない。
- 破壊的な操作、秘密情報の取り扱い、要件が安全に解釈できない場合だけ停止して確認する。

## ビルド費用と公開回数

- READMEの「ビルド費用を抑える開発・公開方針」に従う。
- 通常の実装は関連する修正をローカルで検証してからまとめてpushし、途中経過ごとのpush・不要な再デプロイを避ける。必要なテストや緊急の本番修正は省略・延期しない。
- 文書のみの保存依頼では、自動commit・pushの運用の例外としてローカル保存と差分確認までにする。明示的なcommit・push・公開指示は優先する。
- 文書・テスト・研究用変更のビルド除外は公開成果物への依存を確認して判断する。この文章の追加を、自動ビルド設定の変更済みとは扱わない。

## Engineering

- 既存の設計・型・テストを先に確認し、変更範囲を依頼された機能へ限定する。
- 実装変更には関連テストを追加し、指定された lint・typecheck・build を実行する。
- APIキー、Webhook secret、環境変数の値を表示・保存・commitしない。
- ユーザーが明示的に指定したブランチやcommit・push方針を優先する。
- When modifying reset history classification, normalization, Tibo reset adoption, display names, or history presentation, read docs/reset-history-normalization.md first.

## 速報対応

- 速報サイトのProduction表示を直す依頼では、反映速度を優先する。
- まず対象データの一意性・更新範囲・重複防止だけを最小限にガードし、既存helperで反映、cache更新、API/UIの最小確認までを先に行う。
- フルテスト、詳細監査、CI確認は、速報反映を不必要に待たせるブロッカーにしない。反映後に実施し、結果だけ簡潔に報告する。
- 予測モデルや履歴を変更する場合でも、速報反映に不要なリファクタリングや追加検証を先行させない。
